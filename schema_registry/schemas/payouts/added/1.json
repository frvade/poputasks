{
  "$schema": "http://json-schema.org/draft-04/schema#",

  "title": "Payouts.Added.v1",
  "description": "json schema for BE task events (version 1)",

  "definitions": {
    "event_data": {
      "type": "object",
      "properties": {
        "public_id": {
          "type": "string"
        },
        "amount": {
          "type": "number"
        },
        "transaction_public_ids": {
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "user": {
          "$ref": "#/definitions/user"
        }
      },
      "required": [
        "public_id",
        "amount",
        "user",
        "transaction_public_ids"
      ]
    },
    "user": {
      "type": "object",
      "properties": {
        "public_id": {
          "type": "string"
        }
      },
      "required": [
        "public_id"
      ]
    }
  },

  "type": "object",

  "properties": {
    "event_id":      { "type": "string" },
    "event_version": { "enum": [1] },
    "event_name":    { "enum": ["PayoutAdded"] },
    "event_time":    { "type": "string" },
    "producer":      { "type": "string" },

    "data": { "$ref": "#/definitions/event_data" }
  },

  "required": [
    "event_id",
    "event_version",
    "event_name",
    "event_time",
    "producer",
    "data"
  ]
}
